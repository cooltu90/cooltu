plugins {
    id 'java-library'
    id 'maven-publish'
}
group = COOLTU_PKG + '.processor'
version = COOLTU_VERSION

afterEvaluate {
    publishing {
        publications {
            // Creates a Maven publication called "release".
            release(MavenPublication) {
                from components.java
                groupId = COOLTU_PKG + '.processor'
                artifactId = 'cooltu_processor'
                version = COOLTU_VERSION
            }
        }
    }
}

java {
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation 'com.google.auto.service:auto-service:1.0-rc6'
//    compileOnly files(org.gradle.internal.jvm.Jvm.current().getToolsJar())
    compileOnly files("${System.properties['java.home']}/../lib/tools.jar")

    implementation project(':cooltu_constant')
    implementation project(':cooltu_processor_annotation')

    implementation 'com.github.cooltu90:cooltu4j:1.0.3'

}


